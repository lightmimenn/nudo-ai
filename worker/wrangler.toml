name = "nudoai-api"
main = "src/index.js"
compatibility_date = "2025-12-30"

[vars]
# Разрешённый ORIGIN (важно: origin = домен, без /repo)
ALLOWED_ORIGINS = "https://lightmimenn.github.io"

# Модель по умолчанию (твоя цель):
DEFAULT_MODEL = "gpt-4o"

# Лимиты
MAX_OUTPUT_TOKENS = "2000"
MAX_INPUT_CHARS = "8000"
RATE_LIMIT_PER_MIN = "10"

